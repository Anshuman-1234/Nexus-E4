{
    "name": "soa-library-system",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "npm install && cd Frontend && npm install && cd ../Backend && npm install",
        "build": "cd Frontend && npm install && npm run build"
    }
}